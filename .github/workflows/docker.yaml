name: Docker

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Just print hello
      run: echo "Hello World! The environment is OK!"
